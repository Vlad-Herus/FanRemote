{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Backend",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/HttpService/FanRemote.csproj",
            "preLaunchTask": "build All", 
        }
    ]
}